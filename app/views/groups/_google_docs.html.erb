<% if !@google_docs.empty? %>
  <div id="google_docs" class="activity_box">
  	<h2><%= _('Shared Documents') %></h2>
	  <ul>
	    <% @google_docs.each do |doc| %>
        <li><%= link_to(doc.title, group_shared_entry_path(@group, doc)) %></li>
        <% end %>
	  </ul>
  </div>
<% end %>