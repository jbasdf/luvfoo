<div id="latest-news" class="activity_box">
	<h2><%= _('Latest News') %></h2>
	<% if is_admin? -%>
  	<p><%= link_to _('Add News Item'), new_user_blog_path(current_user) %></p>
  <% end %>
	<%= render :partial => 'news/news_item', :collection => NewsItem.latest_news_from('administrator', 10) -%>
</div>