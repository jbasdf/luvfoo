<% if !@user.groups.empty? %>
  <div id="user_groups" class="activity_box">
  	<h2><%= _('Groups') %> <span><%= link_to _('Create a New Group'), new_user_group_path(current_user) %></span></h2>
    <%= render :partial => 'groups/group_small', :collection => @user.public_groups %>
  </div>
<% end %>