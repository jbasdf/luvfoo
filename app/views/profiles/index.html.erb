<% if @query.nil? %> 
  <% cache(:controller => 'profiles', :action => 'index', :browse => @browse, :index => @alpha_index, :page => @page, :per_page => @per_page, :locale => locale) do %>
    <%= render :partial => 'index_body' %>
    <% end %>
<% else %>
  <%= render :partial => 'index_body' %>
<% end %>


